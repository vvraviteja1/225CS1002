{
  "name": "logging_middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware for Affordmed project",
  "main": "index.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}